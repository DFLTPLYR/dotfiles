import QtQuick

import qs.config

Item {
    property StyledConfig mainconfig: StyledConfig {}
    Rectangle {
        id: main
    }

    Rectangle {
        id: background
    }

    Rectangle {
        id: intersection
    }
}
